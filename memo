seederの使い方　
1.Seederの作成                          　 php artisan make:seeder テーブル名Seeder
2.run()にデータを記述             　　　　 migrationのTableを参考にしてnullableがついている項目をSeederファイルに記述する（eloquent DBファサードの２タイプあるみたい）
3.model名Factory.phpの更新　　　　　　　　 $モデル名s= factory(App\User::class,ダミーを作成する数値)->create
4.DatabaseSeeder.phpでファイルをコール　　 自動的に追加されてたが確認はしよう。リレーションのあるデータベースのダミー作成は作成指示をだしたクラスの名前にする
5.実行　　　　　　　　　　　　　　　　　　 composer dump-autoload してから　php artisan db:seed --class=テーブル（複数形）TableSeeder
6.Tinkerで確認するといい　　　　　　　　　　

検索フォーム
タイトル一覧から検索機能を実装する
必要なこと
ビュー<form>の設定
<form action="CGIプログラムのURI" method="">
  action 必須指定　送信ボタンでデータの送信先の指定
  
  method 非必須　転送方法の指定 post(ユーザー側が情報を送信) or get(サーバー側のデータの取得)
{{ csrf_field }}

where('第一引数','第二引数','第三引数') 3つの引数を受け取る 第一引数　カラム名　第二　演算子　第三　カラム名に対する値

